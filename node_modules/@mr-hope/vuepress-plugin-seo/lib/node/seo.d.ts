import type { PageSeoInfo, SeoContent, SeoOptions } from "../types";
export declare const generateSeo: (options: SeoOptions, base: string, { page, site, locale, path, themeConfig }: PageSeoInfo) => SeoContent;
