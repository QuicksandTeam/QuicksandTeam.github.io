'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var index = (function (arg) {
  var year = new Date().getFullYear();
  var returnString = '';

  if (!arg || Object.keys(arg).length === 0) {
    returnString = "Copyright \xA9 " + year;
  }

  if (typeof arg === 'string') {
    returnString = "Copyright \xA9 " + year + " " + arg;
  }

  if (typeof arg === 'object') {
    var name = arg.name,
        startYear = arg.startYear,
        endYear = arg.endYear,
        htmlEntities = arg.htmlEntities,
        _short = arg["short"];
    var symbol = htmlEntities ? '&copy;' : '©';
    var startYearString = startYear ? startYear + "-" : '';
    var endYearString = endYear ? endYear : year;
    var nameString = name ? " " + name : '';
    returnString = "" + (_short ? '' : 'Copyright ') + symbol + " " + startYearString + endYearString + nameString;
  }

  return returnString;
});

exports.default = index;
//# sourceMappingURL=copyright.cjs.development.js.map
